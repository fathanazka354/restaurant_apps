import 'package:flutter/cupertino.dart';

class ReparasiPage extends StatelessWidget {
  static const routeName = '/reparasi_page';
  const ReparasiPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Maaf sedang Perbaikan'),
    );
  }
}
